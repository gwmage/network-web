{
  "builds": [
    {
      "src": "network-web/package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "network-web/build"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/"
    }
  ]
}